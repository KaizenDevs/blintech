<%= render 'shared/bootstrap_table' %>
<div style="height: 150px"></div>
<div class="container">
  <h1>Listando Categorías</h1>
  <p>Crea categorías que asignarás a las noticias. Esto lo podrás hacer en la página de creación de las noticias</p>
  <%= link_to ' Nueva Categoría', new_category_path, class: 'fa fa-plus btn-edit' %>

  <table class="table">
    <tr>
      <th>Categoría</th>
      <th colspan="2"></th>
    </tr>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= link_to "<span class='fa fa-pencil'></span>".html_safe, edit_category_path(category) %></td>
        <td><%= link_to "<span class='fa fa-trash'></span>".html_safe, category_path(category), method: :delete, data: { confirm: '¿Esta seguro que desea eliminar esta categoría? Recuerda que si lo haces cualquier noticia asociada a esta será eliminada' } %></td>
      </tr>
    <% end %>
  </table>
</div>
<div style="height: 150px"></div>
